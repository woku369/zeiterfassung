name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Package skill files
        run: |
          zip -r brewery-skill.zip . \
            -x ".git/*" \
            -x ".claude/*" \
            -x ".github/*"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: brewery-skill.zip
          generate_release_notes: true
